spring:
  application:
    name: ProductManagement
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://integrator-7592375.okta.com/oauth2/default
  data:
    mongodb:
      uri: mongodb+srv://abhey743_db_user:eO1hGvCJeJVkgXTa@cluster0.yibflne.mongodb.net/?appName=Cluster0
      database: Products


eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone:  http://localhost:8761/eureka

server:
  port: 8087

logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.security.web: DEBUG
    org.springframework.security.oauth2: DEBUG
